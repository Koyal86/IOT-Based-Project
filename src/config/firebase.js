// Firebase configuration has been removed as the application has migrated to local storage.
// This file is kept to prevent import errors during transition but exports nothing meaningful.

export const auth = null;
export const db = null;
export const realtimeDb = null;
export default null;